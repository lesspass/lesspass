{
  "name": "lesspass-web-component",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "cypress:open": "CYPRESS_BASE_URL=http://localhost:5173 cypress open",
    "cypress:headless": "CYPRESS_BASE_URL=http://localhost:4173 cypress run --headless --browser chrome",
    "test": "start-server-and-test dev http://localhost:5173 cypress:open",
    "test:ci": "start-server-and-test 'yarn build && yarn preview' http://localhost:4173 cypress:headless"
  },
  "dependencies": {
    "autoprefixer": "^10.4.14",
    "lesspass": "workspace:*",
    "postcss": "^8.4.21",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tailwindcss": "^3.3.1"
  },
  "devDependencies": {
    "@testing-library/cypress": "^9.0.0",
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "@vitejs/plugin-react": "^3.1.0",
    "cypress": "^12.15.0",
    "start-server-and-test": "^2.0.0",
    "typescript": "^4.9.3",
    "vite": "^4.2.0"
  }
}
