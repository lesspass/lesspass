{
  "name": "lesspass-cozy",
  "version": "1.0.1",
  "cozy-type": "static",
  "cozy-displayName": "LessPass",
  "icon-path": "icon.png",
  "author": "Guillaume Vincent <guillaume@oslab.fr>",
  "description": "lesspass cozy app",
  "main": "src/app.js",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "build": "gulp",
    "prebuild": "npm prune && npm install && gulp"
  },
  "engines": {
    "node": ">=4.2.6"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.14.0"
  },
  "devDependencies": {
    "lesspass-pure": "^1.2.2",
    "gulp": "^3.9.1"
  }
}
