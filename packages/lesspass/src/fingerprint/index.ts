import {
  createFingerprint,
  Fingerprint,
  Finger,
  FingerprintIcon,
  FingerprintColor,
} from "./fingerprint";

export { createFingerprint };

export type { Fingerprint, Finger, FingerprintIcon, FingerprintColor };
