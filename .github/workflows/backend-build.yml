name: 🧩 BACKEND IMAGE BUILD

on:
  push:
    branches: [ "main" ]
    paths:
      - "backend/**"
  workflow_dispatch:
env:
  ACTIONS_RUNNER_DEBUG: true
  ACTIONS_STEP_DEBUG: true
jobs:
  build:
    uses: rhincodon-studio/public-devops-templates/.github/workflows/build-kaniko-tpl.yml@v1.2.1
    with:
      repository: lesspass-backend
      container_repository: balnibarbian/lesspass-backend
      working_directory: backend
      dockerfile_path: Dockerfile
    secrets: inherit
